import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary UI
  static const Color accent = Color(0xFF6E5AAE);
  static const Color ink = Color(0xFF1D1B20);

  // Cards / surfaces
  static const Color card = Color(0xFFF1E9F0);

  // Background gradient
  static const Color bgTop = Color(0xFFF8F3F9);
  static const Color bgBottom = Color(0xFFF2EAF4);

  // Neutral
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color onAccent = Colors.white;

  // Dice
  static const Color diceFace = Colors.white;
  static const Color dicePip = Colors.black;
}
